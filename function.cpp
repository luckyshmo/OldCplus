#include <iostream> 
#include <fstream>
#include <conio.h>
#include <math.h>
#include <cmath>
#include <vector>
#include <cstdlib>
using namespace std;
int main ()
{ 
    int a,i,n;
    vector <int> ar;
	ifstream inputFile ("inf.txt");
	cin>> n;
	ar.resize (n);
	for (i=0; i<n;i++ )
	{
		cin>>a;
		ar.push_back (a);
	}
	for (i=0;;i++)
	{
		ar[i]
	}
    _getch ();
    return 0;
}